import { MarginDirectiveDirective } from './margin-directive.directive';

describe('MarginDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new MarginDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
